/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    //appDir: true,
  },
  images: {
    domains: [],
  },
  // Enable React strict mode
  reactStrictMode: true,
}

module.exports = nextConfig